ccli login -u <username> -p <password>
ccli showCourses
ccli showCourse -i <couseId>
ccli addCouse
ccli patchCourse -i <courseId>
ccli deleteCourse i <courseId>
ccli showLesson -i <lessonId>
ccli addOrUpdateLesson -c <courseId> -j <json>
ccli deleteLesson -i <lessonId>
ccli addResource -i <les sonId> -j <json>
ccli editResource -i <lessonId> -r <resourceId> -j <json>
ccli deleteResource -i <lessonId> -r <resourceId>
ccli addComment -i <lessonId> -i <json>
ccli deleteComment -i <lessonId> -c <commentId>
ccli patchComment -i <lessonId> -c <commentId>
